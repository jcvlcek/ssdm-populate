/**
 * Provides the classes necessary to load United States Social Security
 * Death Index master files into a database, and execute queries against that database. 
 * <p>
 * The project currently supports MySQL and SQL Server databases.  It also
 * implements a trivial, in-memory "database" for testing purposes.
 */
package socialSecurityDeathIndex;